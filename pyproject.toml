[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fire>=0.7.1",
    "flake8>=7.3.0",
    "llm-sdk",
    "mypy>=2.3.1",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "torch>=2.14.0",
    "tqdm>=4.70.0",
]

[tool.uv.sources]
llm-sdk = {path="llm_sdk", editable = true}

[tool.mypy]
exclude = [
  "src/llm_sdk",
]
